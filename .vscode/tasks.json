{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "ehco project path",
			"command": "echo /workspaces/${project}",
		},
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "python",
			"dockerBuild": {
				"tag": "userstorymapper:latest",
				"dockerfile": "${workspaceFolder}/Dockerfile",
				"context": "${workspaceFolder}",
				"pull": true
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"python": {
				"file": "main.py"
			}
		}
	]
}